+++
title = "photos"
description = "things i've seen recently."
+++

a running dump of images i liked enough to keep. click any of them to see it bigger.

to add your own, drop an image file into `content/photos/` (e.g. `content/photos/my-shot.jpg`).
optional captions live in `data/photos.toml`, keyed by filename.
